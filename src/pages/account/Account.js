import { NavbarApp } from '../../components/navbar/NavbarApp';
import './Account.css';

export const Account = () => {
    return (
        <section>
            <NavbarApp />
            Account
        </section>
    );
};

export default Account;