import { NavbarApp } from '../../components/navbar/NavbarApp';
import './Options.css';

export const Options = () => {
    return (
        <section>
            <NavbarApp />
            Options
        </section>
    );
};

export default Options;