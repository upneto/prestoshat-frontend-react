import { NavbarApp } from '../../components/navbar/NavbarApp';
import './Search.css'

export const Search = () => {
    return (
        <section>
            <NavbarApp />
            Search
        </section>
    );
};

export default Search;